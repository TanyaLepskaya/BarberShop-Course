let btnsPopup = document.querySelectorAll(`.pupup__btn`)
let modal = document.querySelectorAll(`.modal`);

btnsPopup.forEach((btn) => {
    btn.addEventListener(`click`, (e) => {
        btn
    })
})
